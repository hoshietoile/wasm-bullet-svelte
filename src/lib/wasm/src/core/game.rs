use crate::core::render;

pub struct Manager {
    // disks: Vec<f64>,
    // pub render_manager: render::Manager,
}

impl Manager {
    pub fn new() -> Self {
        Self {
            // render_manager: render::,
        }
    }

    // pub fn assign_render_manager(&mut self, render_manager: render::Manager) {
    //     self.render_manager = Some(&render_manager);
    // }
}
